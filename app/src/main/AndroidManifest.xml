<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.myaccountbook104">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mainpage" />
        <activity android:name=".registerpage" />
        <activity android:name=".analyzepage"></activity>
        <activity android:name=".add_new_record_time" android:theme="@style/MyDialogStyleBottom" />
        <activity android:name=".Add_new_record_event" android:theme="@style/MyDialogStyleBottom" />
    </application>

</manifest>